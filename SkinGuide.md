> **このガイドは v2.14.2 時点の内容です。**
> 常に最新版はこのファイル（配布リポジトリの `SkinGuide.md`）を参照してください。
> 過去に Release として公開していた「Skin Creation Guide」「Generation Prompt」
> （タグ `Skin_Creation_Guide_v2.2.0` / `Generation_Prompt_v2.2.0`）は
> **v2.2.0 当時の内容で古い**ため、こちらを正としてください。

# スキン作成ガイド

EVA Clock:Rebirth に新しいスキン（パネルデザインの差し替えセット）を追加するためのガイドです。

---

## スキンの作り方は 2 通り

スキンの実体は **PNG 画像（必須 28 + 任意 4）+ 1 個の `layout.json`** です。
画像を用意したあと、各パーツの配置座標を記述した `layout.json` を作る必要があります。
この `layout.json` の作り方に 2 つの方法があります。

| 方法 | 向いている人 | 概要 |
|------|------------|------|
| **A. AI 支援で生成（推奨・らくちん）** | 座標を手で測りたくない人 | 完成イメージの合成画像（`ALL.png`）と各パーツ画像を AI（Claude 等）に渡し、専用プロンプトで `layout.json` を自動生成させる |
| **B. 手動で作成** | 細かく調整したい人 / AI を使わない人 | 各パーツの座標・サイズを自分で測って `layout.json` を記述する |

- **方法 A** の手順とコピペ用プロンプトは本ガイド末尾の
  [付録: AI 支援で layout.json を生成する](#付録-ai-支援で-layoutjson-を生成する) を参照してください。
- どちらの方法でも、**必要な画像ファイルと `layout.json` の仕様は共通**です。以下でまず仕様を説明します。

---

## 配置場所

```
EVAClock/Resources/skins/<スキン名>/
```

例：`EVAClock/Resources/skins/unit02/`

> **同梱スキン：** `Legacy`（旧デザイン）、`Rebirth`（デフォルト）、`Steampunk`、`Star Trek`、`NixieTube`

---

## 必要なファイル一覧

### PNG 画像（必須 28 ファイル + 任意 4 ファイル）

#### パネル・背景（1）

| ファイル名 | 用途 | 備考 |
|-----------|------|------|
| `panel.png` | ウィンドウ背景・形状マスク | 透過部分はクリック透過。サイズはこの画像の比率で全体が決まる |

#### 大きい数字（11） — 時/分の表示

| ファイル名 | 用途 |
|-----------|------|
| `0L.png` 〜 `9L.png` | 0〜9 の数字（時の一の位、分の十の位、分の一の位）|
| `1hL.png` | 時の十の位（常に "1" のみ表示）|

#### 小さい数字（10） — 秒の表示

| ファイル名 | 用途 |
|-----------|------|
| `0s.png` 〜 `9s.png` | 0〜9 の数字 |

#### コロン（2）

| ファイル名 | 用途 |
|-----------|------|
| `SEC_L.png` | 大きいコロン（時:分の区切り）|
| `SEC_s.png` | 小さいコロン（分:秒の区切り）|

#### オーバーレイ（必須 2 + 任意 2）

| ファイル名 | 用途 | |
|-----------|------|---|
| `status.png` | 速度ステータスバー（SNAIL / SLOW / NORMAL / RACING のラベル）| 必須 |
| `danger.png` | バッテリー危機時のフラッシュ表示 | 必須 |
| `title.png` | 銘板（タイトルロゴ）| **任意** |
| `main.png` | 銘板（メインパネルラベル）| **任意** |

**銘板 2 枚は置かなくて構いません（v2.14.0〜）。** 使わない場合は
**PNG も `layout.json` の `overlays.title` / `overlays.main` も丸ごと省略**してください。
片方だけ使うこともできます。

> ⚠️ **`layout.json` に項目を書いたまま PNG だけ消すのは NG です。**
> 必須アセットは「1 枚作り忘れても真っ白にならない」よう他スキンの同名画像へ
> フォールバックする仕組みがあり、その場合**別のスキンの銘板が表示されます**。
> 使わないなら PNG と `layout.json` の項目を**セットで**省いてください。

#### 電源バッジ（2）

| ファイル名 | 用途 |
|-----------|------|
| `IN.png` | 内蔵電源バッジ |
| `EX.png` | 外部電源バッジ |

#### 背景画像（任意・1）

| ファイル名 | 用途 |
|-----------|------|
| `background.png`（任意） | パネル本体の上に全面表示する背景画像。`layout.json` の `background.image` で指定する |

#### 数字の背面画像（任意・1）

| ファイル名 | 用途 |
|-----------|------|
| 任意の名前（任意） | 数字スロット 6 個の背面に敷く画像。`layout.json` の `digitBackdrop.image` で指定する |

---

## 設定ファイル（1）

### `layout.json`

各画像の配置座標とサイズを定義します。
すべての座標は `panel.referenceWidth × panel.referenceHeight` ポイント空間での絶対値です。

**テンプレート：**

```json
{
  "name": "unit02",
  "panel": {
    "referenceWidth": 630.72,
    "referenceHeight": 270.0
  },
  "windowSizes": {
    "small":  { "width": 493,  "height": 211 },
    "normal": { "width": 986,  "height": 422 }
  },
  "digits": {
    "hourTens":    { "x":  54.00, "y":  63.84, "w": 14.16, "h": 122.64 },
    "hourUnits":   { "x":  74.88, "y":  54.02, "w": 77.30, "h": 140.90 },
    "colonHM":     { "x": 160.08, "y":  88.32, "w": 13.00, "h":  71.30 },
    "minuteTens":  { "x": 181.38, "y":  53.02, "w": 77.30, "h": 141.10 },
    "minuteUnits": { "x": 265.38, "y":  53.02, "w": 77.30, "h": 141.10 },
    "colonMS":     { "x": 348.48, "y": 113.82, "w":  9.80, "h":  53.80 },
    "secondTens":  { "x": 364.08, "y":  89.82, "w": 59.50, "h":  98.60 },
    "secondUnits": { "x": 424.98, "y":  90.22, "w": 59.50, "h":  98.60 }
  },
  "overlays": {
    "title":  { "x":   7.20, "y":   6.00, "naturalWidth": 351.6, "naturalHeight":  53.5 },
    "main":   { "x": 492.00, "y":  96.48, "naturalWidth": 130.3, "naturalHeight":  39.4 },
    "status": { "x": 104.64, "y": 219.36, "naturalWidth": 348.0, "naturalHeight":  13.9 },
    "danger": { "x": 492.24, "y": 206.16, "naturalWidth": 130.3, "naturalHeight":  50.2 }
  },
  "badges": {
    "in": { "x": 493.68, "y":  33.84, "w": 127.2, "h": 60.2 },
    "ex": { "x": 493.68, "y": 138.52, "w": 127.2, "h": 60.2 }
  },
  "statusHighlight": {
    "snail":  { "x":  95.76, "w": 75.84 },
    "slow":   { "x": 189.60, "w": 76.08 },
    "normal": { "x": 283.92, "w": 75.84 },
    "racing": { "x": 378.72, "w": 75.84 },
    "y": 235.70,
    "h": 13.92
  },
  "dangerFlash": {
    "x": 493.68, "y": 208.08, "w": 127.2, "h": 46.8
  },
  "background": {
    "image": "background",
    "color": "#000000"
  }
}
```

`background` セクションは任意です。省略すれば背景は透明のまま（パネル設計のみが見える）になります。

---

## layout.json 各セクションの説明

| セクション | 内容 |
|-----------|------|
| `name` | スキン名（任意・表示用）|
| `panel.referenceWidth/Height` | パネル画像の自然サイズ（pt）。**全座標値の基準** |
| `windowSizes.normal` | **100% スケール時のウィンドウ寸法**。Size スライダーの基準値（必須） |
| `windowSizes.small` | 参考値として定義可能（現バージョンでは未使用） |
| `digits.*` | 時刻数字8つ（時十・時一・H:Mコロン・分十・分一・M:Sコロン・秒十・秒一）の描画矩形 |
| `overlays.*` | タイトル等4つの重ね画像の位置と自然サイズ（描画時に伸縮）|
| `badges.in/ex` | IN/EX 電源バッジの描画矩形 |
| `statusHighlight` | 速度ステータス4ハイライトボックス（snail/slow/normal/racing）と統一 Y 座標・高さ。`mode`（任意）で表現を切り替える |
| `dangerFlash` | DANGER フラッシュ表示の描画矩形 |
| `background`（任意）| パネル背面に描画する背景。詳細は下記 |
| `digitBackdrop`（任意）| 数字スロット 6 個の背面に敷く画像。透明度の影響を受けない。詳細は下記 |

### ウィンドウサイズとスケール

ウィンドウサイズは `windowSizes.normal` を 100% 基準とした **`clockScale`（0.25〜2.0）** で連続的に決定されます。

```
実際のウィンドウサイズ = windowSizes.normal × clockScale
```

- `clockScale = 1.0`（デフォルト）→ `windowSizes.normal` のサイズで表示
- `clockScale = 0.5` → normal の 50% サイズ
- `clockScale = 2.0` → normal の 200% サイズ

スケールはメニューの **Size スライダー**（25%〜200%）でリアルタイムに調整できます。
`clockScale` はスキンごとに独立して保存されます。

> **注意：** v2.2 以前にあった X-Small / Small / Normal / Large / Full という固定サイズ選択は廃止されました。

### 背景指定（`background` セクション）

panel.png の**上**にもう一層、背景画像／色を被せたい場合に使用します。
描画順は `panel.png → background → 数字・オーバーレイ・バッジ等` となるため、
背景の上にさらに数字やバッジが重なって表示されます。

| フィールド | 内容 |
|-----------|------|
| `image` | スキンフォルダ内の画像名（拡張子なし）。`background` を指定した場合は `background.png` が使われる |
| `color` | 背景色。`#RRGGBB` または `#RRGGBBAA` 形式（後者はアルファチャネル付き）|

**優先順位：**
1. `image` が指定されていてファイルが存在すれば、その画像をウィンドウ全面に描画
2. `image` が無く `color` が指定されていれば、その色で塗りつぶし
3. 両方無ければ何も描画しない（panel.png のみが見える）

背景画像に透過部分（アルファ）を含めれば、その箇所から panel.png のデザインが透けて見える構造になります。

### 数字の背面画像（`digitBackdrop` セクション・任意）

```json
"digitBackdrop": { "image": "tube" }
```

指定した画像（例では `tube.png`）が、**数字スロット 6 つ**（時十・時一・分十・分一・
秒十・秒一。コロンは対象外）の背面に敷かれます。配置は数字と同じ
「高さフィット・水平中央寄せ」なので、**画像のアスペクト比を数字 PNG と揃えれば
両者がぴったり重なります**。

**panel.png との使い分けが重要です。**

透明度（Transparency）が掛かるのは **panel.png と背景だけ**で、数字・銘板・バッジは
常に不透過のまま残ります。そのため「数字の器」を panel.png に描くと、透明度を上げた
ときに**器だけが消えて数字が宙に浮きます**。

- 透明度を上げたら**一緒に消えてよい**装飾 → `panel.png` に描く
- 透明度を上げても**数字と一緒に残したい**器 → `digitBackdrop` に分ける

同梱スキンの `NixieTube` と `Steampunk` は、消灯したニキシー管（`tube.png`）をこの
方式で分離しています。数字が表示されない桁（時の十の位など）でも管だけは残り、
透明度を上げても管と数字がセットで残ります。

`Steampunk` では **ガラス管と真鍮ソケットをひとまとめの「バルブ組立体」**として
`tube.png` に入れ、**数字スロットの矩形をその組立体に一致させて**います。
`digitBackdrop` は「スロットの高さに合わせて拡縮し水平中央寄せ」で配置されるため、
**スロット＝背面画像の矩形にしないと絵柄が揃いません**。大きさの違う管（時分の大管と
秒の小管）で 1 枚の画像を共用するときは、**小管を大管の相似縮小にする**とアスペクトが
一致して破綻しません。

#### 「枠だけ残して背景を透かす」— 銘板をパネル全面に広げる

数字の器だけでなく **パネルの枠・装飾そのもの**を透明度から守りたいことがあります。
その場合は **`title` 銘板をパネル全面に広げて**、枠の描画をまるごとそこへ置きます。
銘板は `panelOpacity` の影響を受けないため、これだけで「背景の黒は透けるのに、枠は
不透過のまま」が実現できます。**アプリ側の変更は要りません。**

```jsonc
"overlays": {
  // 銘板をパネル全面に = title.png に枠の描画一式を焼き込む
  "title": { "x": 0, "y": 0, "naturalWidth": 660, "naturalHeight": 290 },
  ...
}
```

- `panel.png` … 背景の面（透明度が掛かる）
- `title.png` … 枠・装飾・銘板文字（透明度が掛からない）

同梱スキンの `Star Trek` がこの方式です。LCARS のバー・エルボー・ラベル・EPS 窓の枠を
すべて `title.png` に置き、`panel.png` は黒い面だけにしてあります。透明度を上げると
黒が消えて LCARS の枠と数字だけがデスクトップに残ります。

**バーの区切りのような「隙間」は、黒で塗らずに打ち抜いて（完全透明にして）ください。**
黒で塗ると背景が透けても隙間だけ黒く残り、そこだけ浮いて見えます。打ち抜いておけば
`panel.png` の透明度がそのまま隙間にも反映されます。生成スクリプトなら
`CGContext.clear(_:)`、画像編集なら消しゴム／アルファ 0 での塗りに相当します。

一方、**バー上に載る黒文字のように「背景色と同じ色でも、下地の色と重なって意味を持つ」
要素は枠側（`title.png`）に描いてください。** 背景側に置くと透明度を上げたときに文字が
消えてしまいます。

> 判断基準: **その要素の下に色バーがあるか**。バーの上に載るもの（文字・記号）は枠側に
> 描く。バーを分断するもの（区切りの隙間）は打ち抜く。

なお `title` は本来「銘板」のスロットですが、上記のようにパネル全面を占めても構いま
せん。レイヤー順は `panel` → 背景 → `title` → `main` → `status` → バッジ → 数字 →
ステータス → メーターなので、全面に広げても数字やメーターを隠しません。

### ステータスの表現モード（`statusHighlight.mode`・任意）

現在の速度（SNAIL / SLOW / NORMAL / RACING のどれか）の見せ方を 2 通りから選べます。

```json
"statusHighlight": {
  "mode": "text",
  "snail": { "x": 110.0, "w": 116.0 },
  ...
}
```

| `mode` | 見え方 | 向いているデザイン |
|--------|--------|------------------|
| 未指定（既定） | セルを**矩形で塗る**。非アクティブ = 黒、アクティブ = 赤系 | 計器パネル風。枠や区画がある外観 |
| `"text"` | セルを塗らず、`status.png` のラベル形状で**文字色だけ**を変える | 余計な面を出したくない外観 |

**それぞれで `status.png` の作り方が変わります。**

- **矩形モード（未指定）**: アプリがセルを上塗りするため、ラベル文字は**明るい色**に
  してください。暗い色だと黒の上塗りに沈んで読めなくなります。なお**アクティブな
  セルの文字は赤の上塗りでほぼ読めなくなります**が、これは仕様でスキン側では
  回避できません。
- **`"text"` モード**: 非アクティブなセルには何も描かれないので、`status.png` の
  ラベル色がそのまま出ます。**通常時に読める色**で作ってください。アクティブな
  セルだけがアプリ側の赤系の文字色に差し替わります。

> ⚠️ **`"text"` モードは `status.png` の「アルファ」をマスクにして色板を抜きます。**
> したがって **`status.png` に描いてよいのは文字だけ**です。ラベルの下地（色板・
> ボタン形・枠など）を `status.png` に含めると、マスクがその面全体になるため
> **文字ではなく面ごと色が変わります**。
>
> 下地を出したいときは **`panel.png` か銘板（`title.png`）側へ描いてください。**
> 同梱の `Star Trek` がこの構成です。LCARS の色ピルを `title.png` に置き、
> `status.png` は文字だけにしてあります。
>
> 矩形モードから移行する場合、アプリはそれまで**全セルに黒 55% を被せて**いた
> （`ClockView.highlightInactiveColor`）ことに注意してください。移行後はこの減光が
> 無くなるので、同じ見た目を保つなら**減光後の色（元の色 × 0.45）を下地に焼き込み**
> ます。

同梱スキンでは `NixieTube` のみが `"text"` を使っています。他の 4 スキン
（Legacy / Rebirth / Steampunk / Star Trek）は `mode` 未指定の矩形モードです。

### 座標系について

- **原点 (0, 0)** はパネルの左上
- **Y 軸は下向き正**（macOS のフリップ座標系を使用）
- 全ての座標値はパネルの自然サイズ（`panel.referenceWidth × panel.referenceHeight`）を基準とした絶対値
- 描画時に実際のウィンドウサイズへスケールされる（`applyDynamicLayout` により常時適用）

### Rect の意味

`{ "x": ..., "y": ..., "w": ..., "h": ... }` は描画矩形を指定します。

**数字（`digits`）の画像はこの矩形いっぱいに引き伸ばされます**（縦横それぞれ独立に伸縮。
アスペクト比の維持はしません）。`digitBackdrop` だけは「高さに合わせて拡縮・水平中央寄せ」
という別の規則で配置されます。

#### ★ 数字 PNG のキャンバスは「スロットと同じアスペクト比」にする

引き伸ばしなので、**キャンバスのアスペクト比とスロットのアスペクト比がずれると数字が
横に潰れます／伸びます**。

```
canvasW / canvasH  ==  slotW / slotH
```

書体から数字を描き起こす場合、**先にグリフの実幅を測ってください**。字幅は書体ごとに
大きく違い、目分量の見積もりは外れます。

1. `CTLineGetImageBounds` で、目標の高さにしたときのグリフ幅を実測する
2. `canvasW = グリフ幅 + ぼかし等の余白 × 2`
3. `slotW = canvasW × slotH / canvasH` でスロット幅を決める（この順序が大事）

**グリフがキャンバスに収まらないと、左右が垂直に切り落とされます。**
同梱の `Star Trek` は当初これを踏み、`HelveticaNeue-CondensedBold` の字幅を
「グリフ高の 0.35〜0.49」と見積もっていたところ実測は **0.63** で、"1" 以外の
すべての数字が左右あわせて 8.8〜12.3pt 欠けていました。

> 検査方法: PNG の左端・右端の列のアルファ最大値を見ます。完全に収まっていれば
> 端は透明（0）になるはずで、**不透明なら切れています**。
> `magick 8L.png -crop 1x588+0+0 +repage -format '%[fx:maxima.a]' info:`
> （7 セグメント風など、意図的に端まで塗るデザインは除く）

### Overlay の `naturalWidth/Height`

オーバーレイは「自然サイズ × ウィンドウスケール」で描画されるため、画像本来の縦横比を保ったまま全体スケールに追従します。

---

## ディレクトリ構成チェックリスト

```
EVAClock/Resources/skins/<スキン名>/
├── layout.json
├── panel.png
├── status.png, danger.png            ← 必須
├── title.png, main.png                ← 任意 (銘板を使うときだけ)
├── IN.png, EX.png
├── SEC_L.png, SEC_s.png
├── 1hL.png
├── 0L.png, 1L.png, 2L.png, 3L.png, 4L.png
├── 5L.png, 6L.png, 7L.png, 8L.png, 9L.png
├── 0s.png, 1s.png, 2s.png, 3s.png, 4s.png
└── 5s.png, 6s.png, 7s.png, 8s.png, 9s.png

合計：32ファイル（PNG 31 + JSON 1）
※ 上記に加えて、任意で `background.png` と `digitBackdrop` 用の画像を置けます。
```

---

## 動作確認

1. 上記ファイルを `EVAClock/Resources/skins/<スキン名>/` に配置する
2. アプリをビルドして起動する（リソースの自動再収集のため、初回はクリーンビルド推奨）
3. メニューバーアイコンまたはアプリメニューから「Skin」サブメニューを開く
4. 新しいスキン名が選択肢に表示されるので選択する

---

## スキン切り替え時の挙動

- 各スキンごとに **ウィンドウ位置・表示スケール（Size）・透過度・Floating・Fixed Position 設定** が個別に保存される
- スキンを戻すと、そのスキンで最後に使っていたスケールと位置が復元される
- スキンに依存しない設定（Chime、Audio Level など）は共通

---

## 付録: AI 支援で layout.json を生成する

座標を手で測る代わりに、AI（Claude など画像を読めるアシスタント）に `layout.json` を
生成させる方法です。各パーツの配置を測定するのが面倒な場合に便利です。

### 準備するもの

1. **パーツ画像**（本ガイドの「必要なファイル一覧」と同じ。必須 28 + 任意 4）
2. **`ALL.png`** — 全パーツを正しい位置に配置した「完成状態の合成画像」。
   `panel.png` と**同じピクセルサイズ**で書き出します（AI が各パーツの位置を
   テンプレートマッチングで特定するための参照に使います）。
   ※ `ALL.png` は layout.json 生成専用で、スキンフォルダには入れません（アプリは使いません）。
3. （任意）`background.png`

### 手順

1. 下の「コピペ用プロンプト」をそのままコピーする
2. AI（Claude 等）に、上記の画像ファイルをすべて添付して、プロンプトと一緒に送る
3. 返ってきた JSON を `layout.json` として保存し、スキンフォルダに置く
4. 本ガイドの「動作確認」に従ってアプリで確認する（ズレがあれば数値を微調整）

### コピペ用プロンプト

````
# EVAClock 新スキン layout.json 生成依頼

## 依頼内容
以下のスキン素材をもとに、EVAClock アプリ用の `layout.json` を生成してください。

## 提供ファイル

### 必須パーツ画像（すべて PNG）
| ファイル名 | 内容 |
|---|---|
| `panel.png` | パネル本体（ウィンドウ形状の基準） |
| `status.png` | ステータスバーオーバーレイ |
| `danger.png` | 緊急表示オーバーレイ |
| `title.png` | 銘板オーバーレイ（**任意**・省略可） |
| `main.png` | 銘板オーバーレイ（**任意**・省略可） |
| `IN.png` | 内部電源バッジ |
| `EX.png` | 外部電源バッジ |
| `SEC_L.png` | 時:分 コロン（大） |
| `SEC_s.png` | 分:秒 コロン（小） |
| `1hL.png` | 時の十の位「1」専用画像（大） |
| `0L.png` ～ `9L.png` | 時・分用 LED 数字（大） |
| `0s.png` ～ `9s.png` | 秒用 LED 数字（小） |

### 合成画像
| ファイル名 | 内容 |
|---|---|
| `ALL.png` | 全パーツを正しい位置に配置した完成状態の合成画像。`panel.png` と同一ピクセルサイズ。 |

### 背景（任意）
| ファイル名 | 内容 |
|---|---|
| `background.png` | パネルの上に重ねて描画される背景画像（不要な場合は省略） |

### 数字の背面画像（任意）
| ファイル名 | 内容 |
|---|---|
| 任意の名前 | 数字スロット 6 個の背面に敷く画像（不要な場合は省略）。数字 PNG と同じアスペクト比で作ると両者がぴったり重なる |

## 座標系の定義
- **基準空間**: `panel.png` のピクセルサイズを DPI 変換した「ポイント（pt）空間」
- **DPI 変換式**: `pt = px / DPI * 72`（DPI は panel.png のメタデータから取得。不明な場合は 72 と仮定し px = pt として扱う）
- **原点**: パネル左上が (0, 0)、Y 軸は下向き正
- **すべての座標値はパネル相対 pt 値**

`referenceWidth` と `referenceHeight` = panel.png を pt 換算したサイズ

## 各値の測定方法

### digits（LED 数字・コロンの描画領域）
`ALL.png` 内で各パーツ画像をテンプレートマッチング（OpenCV `matchTemplate` 推奨）し、
マッチ位置のピクセル座標を pt に変換する。

測定対象:
- `hourTens`    : `1hL.png` のマッチ位置（時の十の位「1」）
- `hourUnits`   : `0L.png`〜`9L.png` のうち代表画像のマッチ位置（時の一の位）
- `colonHM`     : `SEC_L.png` のマッチ位置
- `minuteTens`  : 分の十の位（hourUnits の右隣）
- `minuteUnits` : 分の一の位
- `colonMS`     : `SEC_s.png` のマッチ位置
- `secondTens`  : 秒の十の位
- `secondUnits` : 秒の一の位

各 Rect は `{ x, y, w, h }` の形式（左上原点、サイズはパーツ画像の pt サイズ）。

### overlays（オーバーレイ画像の配置）
同様にテンプレートマッチングで `ALL.png` 内の位置を特定する。
`naturalWidth` / `naturalHeight` はパーツ画像自体の pt サイズ（ピクセルサイズを DPI 変換した値）。

### badges（IN / EX バッジ）
`IN.png`・`EX.png` を `ALL.png` でマッチングし Rect を求める。

### statusHighlight（ステータスバーのハイライト領域）
`status.png` 内のハイライトボックス 4 つ（SNAIL / SLOW / NORMAL / RACING）の
x 座標・幅を特定する。Y と H は `status.png` の配置位置から求める。

**`mode`（任意・v2.13〜）** で現在の速度の見せ方を選べる。

| 値 | 表現 | 向いているデザイン |
|---|---|---|
| 未指定（既定） | セルを**矩形で塗る**。非アクティブ = 黒 α0.55、アクティブ = 赤系 | 枠やプレートのあるパネル |
| `"text"` | セルを塗らず、`status.png` のラベル形状で**文字色だけ**を変える | 余計な面を出したくないパネル |

`"text"` のとき非アクティブなセルは何も描かれないため、`status.png` のラベル色が
そのまま見える。逆に既定（矩形）ではラベルが黒 α0.55 で沈むので、ラベルは
**明るめの色**にしておく必要がある（同じ `status.png` を両モードで使い回すなら、
明るめに寄せておくのが無難）。

```json
"statusHighlight": {
  "mode": "text",
  "snail": { "x": 110, "w": 116 },
  ...
}
```

### dangerFlash（緊急フラッシュ矩形）
`danger.png` の配置位置より少し内側の点滅表示領域を Rect で指定する。
（`danger.png` の配置 Rect と同一か、わずかに小さい値）

## 出力する JSON 構造

```json
{
  "name": "<スキン名>",
  "panel": {
    "referenceWidth": <panel.png の pt 幅>,
    "referenceHeight": <panel.png の pt 高さ>
  },
  "windowSizes": {
    "normal": { "width": <px>, "height": <px> }
  },
  "digits": {
    "hourTens":    { "x": 0, "y": 0, "w": 0, "h": 0 },
    "hourUnits":   { "x": 0, "y": 0, "w": 0, "h": 0 },
    "colonHM":     { "x": 0, "y": 0, "w": 0, "h": 0 },
    "minuteTens":  { "x": 0, "y": 0, "w": 0, "h": 0 },
    "minuteUnits": { "x": 0, "y": 0, "w": 0, "h": 0 },
    "colonMS":     { "x": 0, "y": 0, "w": 0, "h": 0 },
    "secondTens":  { "x": 0, "y": 0, "w": 0, "h": 0 },
    "secondUnits": { "x": 0, "y": 0, "w": 0, "h": 0 }
  },
  "overlays": {
    "title":  { "x": 0, "y": 0, "naturalWidth": 0, "naturalHeight": 0 },
    "main":   { "x": 0, "y": 0, "naturalWidth": 0, "naturalHeight": 0 },
    "status": { "x": 0, "y": 0, "naturalWidth": 0, "naturalHeight": 0 },
    "danger": { "x": 0, "y": 0, "naturalWidth": 0, "naturalHeight": 0 }
  },
  "badges": {
    "in": { "x": 0, "y": 0, "w": 0, "h": 0 },
    "ex": { "x": 0, "y": 0, "w": 0, "h": 0 }
  },
  "statusHighlight": {
    "mode": "text",
    "snail":  { "x": 0, "w": 0 },
    "slow":   { "x": 0, "w": 0 },
    "normal": { "x": 0, "w": 0 },
    "racing": { "x": 0, "w": 0 },
    "y": 0, "h": 0
  },
  "dangerFlash": { "x": 0, "y": 0, "w": 0, "h": 0 },
  "background": { "image": "background" },
  "digitBackdrop": { "image": "<背面画像名>" }
}
```

**任意フィールドは 3 つある。不要なら行ごと省略する（`null` でもよい）。**

- `background` … パネル背面の背景。色指定のみの場合は `{ "color": "#RRGGBB" }` とする
- `statusHighlight.mode` … `"text"` を指定すると、セルを矩形で塗る代わりに
  **文字色だけ**が変わる。省略すると従来の矩形モード
- `digitBackdrop` … 数字スロット 6 個の背面に敷く画像（拡張子なしの名前）。
  透明度を上げても数字と一緒に残るので、「消えてほしくない器」に使う

## windowSizes の決め方
`normal` のみ必須。**これがウィンドウサイズ 100% の基準値**となる。
`referenceWidth` / `referenceHeight` のアスペクト比を維持すること。

ユーザーはメニューの **Size スライダー（25%〜200%）** で連続的にサイズを変更できるため、
`normal` 以外のサイズ定義は不要。

```
実際のウィンドウサイズ = normal × clockScale（0.25〜2.0）
```

`normal` の具体的なサイズは用途に合わせて設定する（一般的には 800〜1200px 幅程度）。

## 注意事項
- `hourTens`（`1hL.png`）は画像の x 座標が `hourUnits` より左にあること
- `colonMS`（`SEC_s.png`）は `minuteUnits` の右側にあること（小さいため誤検出注意）
- テンプレートマッチングで信頼度が低い場合は、近傍パーツとの相対位置で補正すること
- 小数点以下 2 桁程度で丸めると読みやすい

## ★ digits の w / h は「数字 PNG のアスペクト比」に合わせること
数字は**スロット矩形いっぱいに引き伸ばして**描画される（縦横それぞれ独立に伸縮）。
そのため次の関係が崩れると数字が横に潰れる／伸びる。

```
数字 PNG の 幅 / 高さ  ==  スロットの w / h
```

`ALL.png` からマッチングで求めた w / h は基本的にこの条件を満たすが、**数字 PNG の
端にグリフが接している場合は要注意**。グリフがキャンバスに収まりきらず左右が
切り落とされている可能性がある。次で検査できる。

```
magick 8L.png -crop 1x<高さ>+0+0 +repage -format '%[fx:maxima.a]' info:
```

左端・右端の列のアルファ最大値が **0 なら収まっている**。**不透明なら切れている**
（7 セグメント風など、意図的に端まで塗るデザインは除く）。切れていた場合は
数字 PNG を作り直す必要があるので、その旨を報告すること。
````

---

## トラブルシューティング

| 症状 | 原因と対処 |
|------|----------|
| スキンメニューに新スキンが出ない | スキンが2個以上ないと表示されない仕様。`Resources/skins/<name>/` にディレクトリがあるか確認 |
| スキンを選択してもデザインが変わらない | `layout.json` がパースエラー。JSON のシンタックスを確認 |
| 数字や画像の位置がずれる | layout.json の座標値が `panel.referenceWidth/Height` の比率と合っていない |
| 一部の画像が表示されない | ファイル名のスペル（大文字小文字含む）を確認。必須 28 個が揃っているか確認 |
| クリックが反応しない領域がある | `panel.png` の透過部分はクリック透過になる仕様。背景を不透明にする |
| Size スライダーで期待通りのサイズにならない | `windowSizes.normal` の値を確認。これが 100% 基準値になる |
